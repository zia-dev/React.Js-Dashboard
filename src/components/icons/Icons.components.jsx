import React from 'react'

const Icons = ({children}) => {
    console.log(children);
  return (
    <span className='className="!text-lg text-purple-600'>
    {children}
    </span>
  )
}

export default Icons;